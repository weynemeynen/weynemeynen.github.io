import React, { useState, useContext, useEffect } from 'react'

// import useFetch from 'hooks/useFetch'
import { ThemeContext } from 'contexts'

import userImage from 'assets/images/logo.png'

import { getDummyUser } from 'dummy'

export default function Card() {
  const themeContext = useContext(ThemeContext)
  const [theme, setTheme] = useState(themeContext)
  // const apiUrl = '/weynemeynen'
  // const [{ response }, doFetch] = useFetch(apiUrl)

  const user = getDummyUser()[0]
  console.log('user', user)


  // useEffect(() => {
  //   doFetch()
  // }, [doFetch])

  return <div className={`card container border-0 bg-${theme}`}>
    <div className="row g-0">
      <div className="col-sm-3 col-md-2" style={{ marginTop: '-1.5em' }} itemScope itemType="http://schema.org/Person">
        <img src={userImage} className="img-fluid border rounded-circle bg-white" alt="..." />
      </div>
      <div className="col-sm-9">
        <div className="card-body">
          <h5 className="card-title">{user.login}</h5>
          <p className="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
          <p className="card-text"><small className="text-muted">Last updated 3 mins ago</small></p>
        </div>
      </div>
    </div>
  </div>
}