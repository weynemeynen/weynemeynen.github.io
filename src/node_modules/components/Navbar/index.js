import React, { useEffect, useContext, useState } from 'react'
import classNames from "classnames";
import { NavLink } from 'react-router-dom';

import { ThemeContext, LIGHT, DARK } from 'contexts'

import './navbar.css'

export default function Navbar() {
  const [theme, setTheme] = useContext(ThemeContext)
  const [show, setShow] = useState(false)

  const handleNavbarToggler = () => setShow(prev => !prev)
  const isShow = show ? 'show' : ''

  useEffect(() => {
    document.body.className = `bg-${theme}`
  }, [theme])

  const iconClasses = classNames({
    'bi-moon-stars': theme === LIGHT,
    'bi-sun': theme === DARK
  })

  const active = classNames({
    [`text-${LIGHT}`]: () => theme === LIGHT ? LIGHT : LIGHT,
  })

  return (
    <nav className={`bg-${DARK} navbar navbar-${DARK} navbar-expand-sm py-2`}>
      <div className="container py-1">
        <button className="navbar-toggler border-0 ps-0" type="button" onClick={handleNavbarToggler}>
          <span className="navbar-toggler-icon"></span>
        </button>
        <div className={`collapse navbar-collapse order-1 order-sm-0 row ${isShow}`} id="navbarSupportedContent">
          <nav className="navbar-nav px-3">
            <NavLink to="/" exact className="nav-link border-bottom border-secondary" activeClassName={active}>Портфолио</NavLink>
          </nav>
        </div>
        <div className={`btn-group col-2 justify-content-end text-${LIGHT}`} role="group">
          <i className={`${iconClasses}`}
            role='button'
            onClick={() => setTheme(prev => prev === DARK ? LIGHT : DARK)}
            title='переключить цвет.тему'></i>
        </div>
      </div>
    </nav>
  )
}